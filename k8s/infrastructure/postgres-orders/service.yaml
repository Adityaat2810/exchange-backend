apiVersion: v1
kind: Service
metadata:
  name: postgres-orders
  labels:
    app.kubernetes.io/name: postgres-orders
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: postgres-orders
